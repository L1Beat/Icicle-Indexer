rpc_cache
*.env
config.json
config.yaml
sync.log
terminals/
clickhouse-metrics-poc
clickhouse-metrics-poc-linux

# AI/LLM context files
CLAUDE.md
.docs/
icicle

# Deployment: track templates / configs, NOT per-server secrets.
# The deploy/.env (matched by *.env above) holds CLICKHOUSE_PASSWORD etc. and stays ignored.
Makefile
